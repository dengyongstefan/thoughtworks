/**
 * Created by dengyong on 7/28/17.
 */
/*
function moveString(Str,offset) {
     let inputStr=Str;
     let result;
    for(i=0;i<offset;i++){
        let strLast = inputStr.slice(Str.length-1);
        let strHead = inputStr.slice(0,Str.length-1);
        result = strLast.concat(strHead);
        inputStr=result;
    }
    return result;
}*/



/*
function largestNumber(Arr) {
    Arr.sort();
    console.log(Arr);
    let result='';
    for(let index in Arr){
      result+=Arr[Arr.length-index-1]
    }
    console.log(result)
}

largestNumber([1,20,23,4,8]);*/
